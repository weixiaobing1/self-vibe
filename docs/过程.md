\# MindFlow AI 项目 - 严格分步开发规则（Claude Code 必须遵守） ## 核心约束（强制） 1. 每次只执行【当前指定步骤】，绝对不提前生成后续内容 2. 每一步完成后必须停止，并输出固定语句：   【步骤已完成，请测试，测试通过后再继续】 3. 所有代码必须可直接运行、无报错、无缺失依赖 4. 固定环境配置（不许修改）   - MySQL：localhost:3306，库名 mindflow，用户名 root，密码 123456   - Redis：localhost:6379，密码 123456   - 项目包名：com.mindflow   - Spring Boot 版本：3.2.5 5. 代码结构清晰、带注释、符合阿里巴巴 Java 开发手册 --- # 总开发路线（5 阶段 + 21 步） 阶段 1：项目骨架搭建（能跑、能连库） 阶段 2：用户系统（登录、注册、JWT） 阶段 3：笔记 + AI 核心功能 阶段 4：复习计划 + 学习统计 阶段 5：文件上传 + 部署打包 --- # 阶段 1：项目基础骨架搭建 ## 步骤 1-1：生成完整 pom.xml（仅生成 pom.xml） 包含： - Spring Web - Spring Data Redis - MySQL Driver - MyBatis Plus 3.5.7 - Lombok - Hutool 5.8.29 - JWT 0.11.5 - Swagger / OpenAPI 3.0 - Jackson 完成后停止。 ## 步骤 1-2：生成 application.yml（仅生成 yml） 包含： - 端口 8080 - MySQL 连接 - Redis 连接 - MyBatis Plus 配置 - JWT 配置 - Swagger 配置 完成后停止。 ## 步骤 1-3：生成全局通用基础类 - 统一返回 Result<T> - 错误码枚举 ErrorCode - 业务异常 BusinessException - 全局异常处理器 GlobalExceptionHandler 完成后停止。 ## 步骤 1-4：生成全部 6 张表实体类 + Mapper 表： user、note、review_plan、interview_question、ai_chat、study_statistics 要求： - Lombok 注解 - MyBatis-Plus 注解 - 逻辑删除、自动填充字段 完成后停止。 ## 步骤 1-5：生成基础配置类 - MyBatis-Plus 配置 - Redis 配置 - Swagger 配置 - 跨域配置 完成后停止。 ## 步骤 1-6：阶段 1 完成检查 项目可正常启动，Swagger 可访问，能连接 MySQL + Redis 完成后停止。 --- # 阶段 2：用户系统（登录注册 JWT） ## 步骤 2-1：生成 JWT 工具类 包含：生成 Token、解析 Token、判断过期 完成后停止。 ## 步骤 2-2：用户注册接口（Controller + Service） 逻辑： - 用户名唯一校验 - 密码 BCrypt 加密 - 入库 user 表 完成后停止。 ## 步骤 2-3：用户登录接口 逻辑： - 校验用户名密码 - 生成 accessToken + refreshToken - Token 存入 Redis 完成后停止。 ## 步骤 2-4：JWT 拦截器 + 登录校验 规则： - /api/user/** 放行 - 其他接口必须登录 完成后停止。 ## 步骤 2-5：获取用户信息接口 /api/user/info 完成后停止。 ## 步骤 2-6：阶段 2 测试 注册 → 登录 → 带 Token 请求 → 获取用户信息 完成后停止。 --- # 阶段 3：笔记模块 + AI 核心 ## 步骤 3-1：笔记创建接口（含 AI 总结） 流程： 用户输入 content → 保存 note → AI 自动总结 → 回填 summary、category、tags、difficulty 完成后停止。 ## 步骤 3-2：笔记列表 / 详情 / 删除 接口 完成后停止。 ## 步骤 3-3：AI 服务封装 AIService 实现： - 笔记总结 - 面试题生成 - 代码解释 完成后停止。 ## 步骤 3-4：AI 面试题生成接口 根据 noteId 生成题目并保存到 interview_question 完成后停止。 ## 步骤 3-5：AI 对话接口（带上下文 session） 完成后停止。 --- # 阶段 4：复习计划 + 学习统计 ## 步骤 4-1：复习计划自动生成（艾宾浩斯算法） 笔记创建后自动生成 review_plan 完成后停止。 ## 步骤 4-2：获取今日复习任务接口 完成后停止。 ## 步骤 4-3：完成复习接口 更新 memory_score、next_review_time 完成后停止。 ## 步骤 4-4：学习统计接口（日/周/趋势） 完成后停止。 --- # 阶段 5：文件上传 + 部署 ## 步骤 5-1：MinIO 文件上传接口（头像/附件） 完成后停止。 ## 步骤 5-2：生成 Dockerfile 完成后停止。 ## 步骤 5-3：生成完整 docker-compose.yml（后端+MySQL+Redis+MinIO） 完成后停止。 ## 步骤 5-4：生成 README.md 启动说明 项目全部完成。 --- # 启动指令（直接发给 Claude） 请严格按照本文档执行，现在从【步骤 1-1：生成 pom.xml】开始，只做这一步，完成后停止并提示我测试。